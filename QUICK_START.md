# 🚀 БЫСТРЫЙ СТАРТ - РП Сервер

## 📋 Что это?

**РП Сервер** - это полная система управления паспортами жителей и штрафами для ролевых игровых серверов. Современный веб-интерфейс с темной темой, полная система авторизации и удобное управление данными.

## ⚡ Супер быстрый запуск (1 команда)

```bash
# Клонируем, настраиваем и запускаем одной командой
git clone <your-repo-url> rp-server && cd rp-server && chmod +x setup.sh start.sh && ./setup.sh && ./start.sh start
```

**Готово!** Система будет доступна через 2-3 минуты:
- 🌐 **Frontend**: http://localhost:3000
- 🔧 **Backend**: http://localhost:8000  
- 📚 **API Docs**: http://localhost:8000/docs
- 👤 **Логин**: admin / admin123

## 🔧 Пошаговая установка

### 1. Предварительные требования
```bash
# Проверьте что у вас установлены:
docker --version          # Docker 20.10+
docker-compose --version  # Docker Compose 2.0+
git --version             # Git
```

### 2. Клонирование и настройка
```bash
# Клонируем проект
git clone <your-repo-url> rp-server
cd rp-server

# Делаем скрипты исполняемыми
chmod +x setup.sh start.sh

# Запускаем автоматическую настройку
./setup.sh
```

### 3. Запуск системы
```bash
# Запуск в продакшн режиме
./start.sh start

# ИЛИ в режиме разработки
./start.sh dev

# ИЛИ используя быстрый запуск
./quick-start.sh
```

### 4. Проверка работы
```bash
# Проверка статуса
./start.sh status

# Просмотр логов
./start.sh logs

# Остановка системы
./start.sh stop
```

## 🎯 Основные функции

### 👥 Для администраторов
- ✅ **Управление пользователями** - Создание полицейских и администраторов
- ✅ **Просмотр всех логов** - Полный аудит действий в системе
- ✅ **Системные настройки** - Конфигурация параметров безопасности
- ✅ **Экспорт данных** - CSV, JSON, Excel, PDF форматы

### 👮 Для полицейских
- ✅ **Управление паспортами** - Создание, редактирование, поиск жителей
- ✅ **Выписка штрафов** - Быстрое оформление с популярными статьями
- ✅ **Статистика** - Графики и аналитика по данным
- ✅ **История действий** - Просмотр своих операций

### 📊 Для всех пользователей
- ✅ **Современный интерфейс** - Темная тема, анимации, адаптивный дизайн
- ✅ **Быстрый поиск** - По всем полям в реальном времени
- ✅ **Детальная информация** - Полные профили жителей с историей штрафов
- ✅ **Безопасность** - JWT токены, роли, логирование

## 🛠️ Полезные команды

### Управление системой
```bash
./start.sh start     # Запуск
./start.sh dev       # Режим разработки  
./start.sh stop      # Остановка
./start.sh restart   # Перезапуск
./start.sh build     # Пересборка и запуск
./start.sh status    # Статус сервисов
./start.sh logs      # Все логи
./start.sh logs backend   # Логи бэкенда
./start.sh logs frontend  # Логи фронтенда
```

### Управление данными
```bash
./start.sh backup          # Резервная копия БД
./start.sh restore backup.sql  # Восстановление БД
./start.sh clean           # Очистка всех данных
./start.sh update          # Обновление системы
```

### Быстрые скрипты
```bash
./quick-start.sh    # Быстрый запуск с инструкциями
./dev.sh           # Запуск в режиме разработки
./clean.sh         # Интерактивная очистка данных
```

## 📱 Интерфейс

### 🎨 Современный дизайн
- **Темная тема** с градиентами и эффектами
- **Адаптивный дизайн** для компьютера, планшета и телефона
- **Плавные анимации** при переходах и загрузке
- **Интуитивная навигация** с боковой панелью

### 📊 Богатая аналитика
- **Интерактивные графики** по возрасту и полу жителей
- **Статистика штрафов** по дням и статьям нарушений
- **Активность пользователей** в реальном времени
- **Тренды и показатели** для принятия решений

### 🔍 Удобный поиск
- **Поиск в реальном времени** по всем полям
- **Продвинутые фильтры** по дате, сумме, статусу
- **Сортировка таблиц** по любому столбцу
- **Экспорт результатов** в разных форматах

## 🔧 Настройка

### Переменные окружения
Основные настройки в файлах `.env`:

**Backend (backend/.env):**
```env
DATABASE_URL=postgresql://rp_user:rp_password@database:5432/rp_server_db
SECRET_KEY=your-secret-key-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

**Frontend (frontend/.env):**
```env
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=РП Сервер
VITE_APP_VERSION=1.0.0
```

### Смена паролей
```bash
# Отредактируйте backend/.env
nano backend/.env

# Измените строки:
ADMIN_PASSWORD=your-new-password
SECRET_KEY=your-new-secret-key

# Перезапустите систему
./start.sh restart
```

## 🚨 Решение проблем

### Порты заняты
```bash
# Проверить что использует порт
sudo lsof -i :8000

# Остановить процесс
sudo kill -9 PID

# Или использовать другие порты в docker-compose.yml
```

### Проблемы с Docker
```bash
# Перезапуск Docker
sudo systemctl restart docker

# Очистка Docker
docker system prune -f

# Пересборка контейнеров
./start.sh build
```

### База данных не подключается
```bash
# Проверка логов базы данных
./start.sh logs database

# Пересоздание базы данных
./start.sh clean
./start.sh start
```

### Frontend не загружается
```bash
# Проверка логов фронтенда
./start.sh logs frontend

# Очистка кэша браузера
# Ctrl+Shift+R или Cmd+Shift+R

# Проверка backend API
curl http://localhost:8000/health
```

## 📞 Поддержка

### Часто задаваемые вопросы

**Q: Как сменить порт?**
A: Отредактируйте `docker-compose.yml`, найдите секцию `ports` и измените `"3000:80"` на `"ВАШ_ПОРТ:80"`

**Q: Как добавить пользователя?**
A: Войдите как администратор → Пользователи → Создать пользователя

**Q: Как сделать резервную копию?**
A: `./start.sh backup` - создаст файл в папке `backups/`

**Q: Забыл пароль администратора?**
A: Отредактируйте `ADMIN_PASSWORD` в `backend/.env` и перезапустите

**Q: Как обновить систему?**
A: `git pull && ./start.sh update`

### Получение помощи
- 📖 **Документация**: Читайте `README.md` и файлы в папке проекта
- 🐛 **Баги**: Создавайте Issues в GitHub репозитории
- 💬 **Вопросы**: Используйте GitHub Discussions
- 📧 **Email**: support@rp-server.com

## 🎉 Готово!

Теперь у вас есть полноценная система управления РП сервером:

1. **Зайдите** на http://localhost:3000
2. **Войдите** как admin / admin123  
3. **Создайте** первого жителя в разделе "Паспорта"
4. **Выпишите** первый штраф в разделе "Штрафы"
5. **Изучите** аналитику в разделе "Главная"

**Приятного использования! 🚀**

---

<div align="center">

**🎮 Сделано с ❤️ для ролевого игрового сообщества**

[![Version](https://img.shields.io/badge/version-1.0.0-blue)](https://github.com/your-repo/rp-server)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE)
[![Docker](https://img.shields.io/badge/docker-ready-blue)](https://docker.com)

</div>